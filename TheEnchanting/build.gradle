dependencies {
    project(':TheEnchanting').subprojects.forEach(project -> implementation project)
    compileOnly 'org.spigotmc:spigot-api:1.15.2-R0.1-SNAPSHOT'
    compileOnly fileTree(dir: 'libs', include: ['*.jar'])
}